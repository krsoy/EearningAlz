function EmptyState({
  message,
}) {
  return (
    <div
      style={{
        padding:
          "30px",
        textAlign:
          "center",
      }}
    >
      {message}
    </div>
  );
}

export default EmptyState;